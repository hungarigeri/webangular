import { IngredientHighlightPipePipe } from './ingredient-highlight-pipe.pipe';

describe('IngredientHighlightPipePipe', () => {
  it('create an instance', () => {
    const pipe = new IngredientHighlightPipePipe();
    expect(pipe).toBeTruthy();
  });
});
