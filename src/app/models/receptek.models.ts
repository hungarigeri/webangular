export interface Receptek {
    id: number;
      title: string;
      prepTime: number;
      cookTime: number;
      difficulty: string;
      image: string;
      tags: [];
      description: string;
}